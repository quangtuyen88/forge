import SwiftUI

/// Widget and Live Activity colors: DESIGN.md's dark tokens, fixed because widgets draw their own dark container.
enum WidgetTheme {
  static let accent = Color(red: 0x0A / 255, green: 0x84 / 255, blue: 0xFF / 255)
  static let done = Color(red: 0x30 / 255, green: 0xD1 / 255, blue: 0x58 / 255)
  static let background = Color(red: 0x1C / 255, green: 0x1C / 255, blue: 0x1E / 255)
}
