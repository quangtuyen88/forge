.build/
.DS_Store
xcuserdata/
DerivedData/
server/node_modules/
server/dist/
build/
App/Secrets.local.xcconfig

# Scratch output from UI review runs.
tmp/

# The feature tour is a build artifact: HEVC straight from the simulator, hundreds of MB,
# and regenerated by `make record-app-tour`. GitHub rejects anything over 100 MB anyway.
artifacts/*.mp4
artifacts/*.jpg
