.build/
.DS_Store
xcuserdata/
DerivedData/
server/node_modules/
server/dist/
server/.dev.vars
build/
App/Secrets.local.xcconfig

# Scratch output from UI review runs.
tmp/

# Feature tour and QA evidence bundles: simulator recordings and screenshots, hundreds of MB,
# regenerated by `make record-app-tour` and per QA run. GitHub rejects anything over 100 MB anyway.
artifacts/

# macOS text clippings dragged into the checkout.
*.textClipping
